- hosts: all
  tasks:
    - name: Install the latest version of apache
      yum
        name: httpd
        state: latest

    - name: Start and enable httpd
      service: 
        name: httpd
        state: started
        enabled: yes
    - name: exec shell
      shell: |
        yum install epel-release -y
        yum install nginx -y      
